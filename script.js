//Create 16x16 pattern of divs

for(divCounter = 0; divcounter <= 16; divCounter++ ){ 
    let gridDiv = document.createElement('div');
    resultsDiv.setAttribute('class', 'grid-container')
    document.body.appendChild(gridDiv); 
}
